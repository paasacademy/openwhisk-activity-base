{
  "name": "Weather",
  "version": "0.1.0",
  "description": "Example demonstrates how to setup a simple HTTP GET endpoint with OpenWhisk that returns the weather.",
  "scripts": {
    "postinstall": "npm link serverless-openwhisk",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}